meta {
  name: Delete Message
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/queues/{{queueName}}/messages
  body: json
  auth: none
}

body:json {
  {
    "receipt_handle": "{{receiptHandle}}"
  }
}